# 📍 第 10 课：分形与迭代（算法生成艺术）

> 分形用简单规则反复生成复杂图案，局部与整体具有相似性。

## 本课要完成什么

1. 用自己的话说清楚：分形用简单规则反复生成复杂图案，局部与整体具有相似性。
2. 看懂并计算：新尺寸 = 旧尺寸 ÷ 缩放倍数
3. 运行一段最小代码，完成：完成3×3基础阵列

## 先把关系说成人话

**数量关系：** 新尺寸 = 旧尺寸 ÷ 缩放倍数

**怎么理解：** 每迭代一次，元素变小但数量增加。

**数字例子：** 边长30按3倍缩小，新方块边长10毫米。

**生活类比：** 像雪花、蕨类和树枝：同一结构在不同尺度重复出现。

## 已验证的课堂最小代码

下面的代码聚焦本课核心知识点，适用于带 Python 控制台的 FreeCAD 桌面版。

```python
import FreeCAD as App
import Part

doc = App.newDocument("Lesson10")
for x in range(3):
    for y in range(3):
        if not (x == 1 and y == 1):
            box = doc.addObject("Part::Feature", "Box")
            box.Shape = Part.makeBox(8, 8, 2)
            box.Placement.Base = App.Vector(x*10, y*10, 0)
doc.recompute()
```

## 按这五步运行

1. 打开FreeCAD，新建文档。
2. 打开“视图 → 面板 → Python控制台”。
3. 先原样运行代码，确认出现模型、控件或输出。
4. 一次只修改一个数字，再运行一次。
5. 记录“改了什么、结果怎样、为什么会变”。

## 常见问题

- 循环层数过多导致运行很慢
- 间距与缩放不匹配
- 对象数量太大难以操作

## 分层任务

### 基础任务

完成3×3基础阵列

### 进阶任务

删除中心位置形成地毯图案

### 创意任务

增加一轮迭代并记录对象数

## 下课前检查

- [ ] 我能用一句话解释本课原理。
- [ ] 我能指出代码中最关键的参数。
- [ ] 我只修改一个参数并比较了结果。
- [ ] 我保存了模型文件、截图和一句话说明。
